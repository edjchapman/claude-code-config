## Summary

<!-- Bold groupings (e.g. **Added**, **Changed**, **Removed**) help reviewers scan. Bullets, not paragraphs. Lead with the WHY when it's non-obvious. -->

-

## Test plan

<!-- Checkbox list — what you ran locally + what you expect CI to verify. Smoke tests for post-merge behavior also go here. -->

- [ ]
- [ ]

## Notes for reviewers

<!-- Optional. Use this to call out: focus areas, intentionally-noisy diffs (e.g. lockfile/auto-fix), trade-offs taken, follow-up work intentionally deferred. Delete the section if there's nothing to add. -->

<!--
Reminder: this repo uses squash-merge with PR_TITLE as the commit subject and PR_BODY as the commit body. Write the title in conventional-commits format (type(scope): description) — the `lint-pr` workflow validates it on every PR update.
-->
