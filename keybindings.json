{
  "bindings": [
    { "key": "ctrl+shift+r", "command": "review", "when": "input" },
    { "key": "ctrl+shift+c", "command": "commit", "when": "input" },
    { "key": "ctrl+shift+p", "command": "pr", "when": "input" },
    { "key": "ctrl+shift+l", "command": "lint", "when": "input" },
    { "key": "ctrl+shift+t", "command": "tdd", "when": "input" }
  ]
}
